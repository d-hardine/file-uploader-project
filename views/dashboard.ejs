<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>Hardine File Sharing Home</title>
</head>
<body>
    <% if (locals.user) { %>
        <h1>Hello <%= user.firstName %>!</h1>
        <a href="/log-out"><button>LOG OUT</button></a>
        <br>
        <br>
        <form action="/upload" method="post">
            <input type="hidden" value=<%= currentFolderInfo.id %> name="currentFolderId">
            <input type="hidden" value=<%= currentUrl %> name="currentUrl">
            <button>Upload File Here</button>
        </form>
        <h3>Current folder: <%= currentFolderInfo.folderName %></h3>
        <% if (locals.currentFolderInfo.folderIdBefore) { %>
            <a href="/dashboard/<%= currentFolderInfo.folderIdBefore %>">back one folder</a>
        <% } %>
        <% if (locals.nextFoldersInfo) { %>
            <ul>
                <% nextFoldersInfo.forEach(nextFolder => { %>
                    <li><a href="/dashboard/<%= nextFolder.id %>">FOLDER: <%= nextFolder.folderName %></a></li>
                <% }) %>
            </ul>
        <% } %>
        <% if (locals.uploadedFiles) { %>
            <ul>
                <% uploadedFiles.forEach(uploadedFile => { %>
                    <li><a href="/download/<%= uploadedFile.id %>"><%= uploadedFile.originalFileName %></a></li>
                <% }) %>
            </ul>
        <% } %>
        <form action="/create-folder" method="post">
            <input type="hidden" value=<%= currentFolderInfo.id %> name="currentFolderId">
            <input type="hidden" value=<%= currentUrl %> name="currentUrl">
            <button>CREATE FOLDER</button>
        </form>
    <% } %>
</body>
</html>